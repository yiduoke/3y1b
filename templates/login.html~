{% extends "base.html" %}
{% block title %}
Log In
{% endblock %}
{% block body %}
    {% block content1 %}
<center>
<h2> Log In </h2>
    <div class = "col-md-5 container login">
      <form method="POST" action="/account/login/post">

      {% with messages = get_flashed_messages() %}
      {% if messages %}
      <div class="alert alert-info alert-dismissible fade show" role="alert">
        {% for message in messages %}
        {{ message }}
        {% endfor %}
      </div>
      {% endif %}

      {% endwith %}
      <div class="form-group">
        <label> Username </label>
        <input type="text" class="form-control" name="username" placeholder="Enter username">
      </div>

      <div class="form-group">
	<label> Password </label>
	<input type="password" class="form-control" name="password" placeholder="Enter password">
      </div>

      <div class = "form-group">
	<input type="submit" name="login" value="Submit" class="btn btn-outline-primary btn-success">
      </div>


    New? <a href="/account/create">Create Account</a>
    </form>
    </div>
</center>
    {% endblock %}
    {% endblock %}
